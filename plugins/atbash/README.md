# Atbash Safety for Codex

This package is the plugin payload for the repository-level Atbash Codex marketplace. It contains the plugin manifest, catch-all `PreToolUse` hook, compiled SDK adapter, and the platform-native binding selected from `@atbash/sdk@0.6.1` during the build.

Use the repository root for installation, configuration, testing, and security guidance.
